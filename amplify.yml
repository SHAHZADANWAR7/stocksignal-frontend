version: 1
frontend:
  phases:
    build:
      commands:
        - rm -rf node_modules package-lock.json
        - npm install --legacy-peer-deps
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
